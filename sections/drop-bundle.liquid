{% if settings.enable_drop_features and settings.drop_bundle_product != blank %}
  <section class="drop-bundle-section" aria-labelledby="DropBundleTitle-{{ section.id }}">
    <div class="drop-bundle-section__inner">
      <p class="drop-bundle-section__eyebrow">{{ 'drop.bundle_page_eyebrow' | t }}</p>
      <h2 id="DropBundleTitle-{{ section.id }}">{{ 'drop.bundle_page_title' | t }}</h2>
      {% render 'drop-bundle-card', home_bundle: true %}
    </div>
  </section>

  <style>
    .drop-bundle-section { padding: 56px 24px; background: var(--color-background, #fff); color: var(--color-foreground, #111); }
    .drop-bundle-section__inner { width: min(100%, 840px); margin: 0 auto; }
    .drop-bundle-section__eyebrow { margin: 0 0 8px; font-size: 11px; font-weight: 700; letter-spacing: .12em; text-transform: uppercase; }
    .drop-bundle-section h2 { max-width: 560px; margin: 0 0 22px; font-size: clamp(1.65rem, 4vw, 3rem); line-height: .98; letter-spacing: -.04em; text-transform: uppercase; }
    .drop-bundle-section .drop-bundle-card { display: grid; grid-template-columns: minmax(190px, .72fr) minmax(0, 1fr); gap: clamp(18px, 4vw, 38px); padding: clamp(16px, 3vw, 26px); overflow: hidden; border: 1px solid var(--color-border, #d8d8d8); background: var(--color-surface, #f5f5f5); }
    .drop-bundle-section .drop-bundle-card__offer { display: flex; grid-column: 1 / -1; width: 100%; align-items: center; justify-content: center; gap: 9px; margin: 0; padding: 8px 12px; background: #8f173d; color: #fff; font-size: 9px; font-weight: 700; letter-spacing: .1em; line-height: 1; text-align: center; text-transform: uppercase; }
    .drop-bundle-section .drop-bundle-card__offer-label > span { margin-right: 3px; color: #f3b15c; }
    .drop-bundle-section .drop-bundle-card__offer-logo { display: block; width: 30px; height: 15px; object-fit: contain; }
    .drop-bundle-section .drop-bundle-card__images { position: relative; width: min(100%, 220px); min-height: 230px; margin: 0 auto; isolation: isolate; }
    .drop-bundle-section .drop-bundle-card__images img { position: absolute; object-fit: contain; filter: drop-shadow(0 10px 12px rgba(0, 0, 0, .14)); }
    .drop-bundle-section .drop-bundle-card__images img:first-child { top: 4px; left: 2%; z-index: 1; width: 72%; height: 82%; transform: rotate(-4deg); }
    .drop-bundle-section .drop-bundle-card__images img:last-child { right: 1%; bottom: 0; z-index: 2; width: 76%; height: 84%; transform: rotate(4deg); }
    .drop-bundle-section .drop-bundle-card__content { display: flex; flex-direction: column; align-items: flex-start; }
    .drop-bundle-section .drop-bundle-card__eyebrow, .drop-bundle-section .drop-bundle-card__copy, .drop-bundle-section .drop-bundle-card__shipping, .drop-bundle-section .drop-bundle-card__status { margin: 0; }
    .drop-bundle-section .drop-bundle-card__eyebrow { font-size: 12px; font-weight: 700; letter-spacing: .09em; }
    .drop-bundle-section .drop-bundle-card__copy { margin-top: 7px; font-size: 15px; line-height: 1.45; }
    .drop-bundle-section .drop-bundle-card__sizes { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 10px; width: 100%; margin-top: 14px; }
    .drop-bundle-section .drop-bundle-card__sizes label { display: grid; gap: 6px; min-width: 0; font-size: 10px; font-weight: 700; letter-spacing: .05em; text-transform: uppercase; }
    .drop-bundle-section .drop-bundle-card__sizes select { width: 100%; min-height: 42px; padding: 8px 28px 8px 10px; border: 1px solid var(--color-border, #999); border-radius: 0; background: var(--color-background, #fff); color: inherit; font: inherit; }
    .drop-bundle-section .drop-bundle-card__shipping { margin-top: 12px; font-size: 12px; font-weight: 700; }
    .drop-bundle-section .drop-bundle-card__button { display: flex; width: 100%; justify-content: space-between; gap: 12px; margin-top: 14px; padding: 12px 14px; border: 0; background: var(--color-foreground, #111); color: var(--color-background, #fff); font: inherit; font-size: 11px; font-weight: 700; letter-spacing: .07em; cursor: pointer; }
    .drop-bundle-section .drop-bundle-card__button:disabled { cursor: not-allowed; opacity: .52; }
    .drop-bundle-section .drop-bundle-card__status { margin-top: 9px; color: var(--color-error, #b91c1c); font-size: 12px; }
    @media (max-width: 640px) { .drop-bundle-section { padding: 48px 18px; } .drop-bundle-section .drop-bundle-card { display: block; padding: 11px; } .drop-bundle-section .drop-bundle-card__images { display: none; } .drop-bundle-section .drop-bundle-card__content { margin-top: 9px; } .drop-bundle-section .drop-bundle-card__sizes { grid-template-columns: repeat(2, minmax(0, 1fr)); margin-top: 12px; } .drop-bundle-section .drop-bundle-card__sizes select { min-height: 38px; padding: 6px 24px 6px 9px; } }
  </style>
{% endif %}

{% schema %}
{
  "name": "Drop bundle",
  "settings": [],
  "presets": [{ "name": "Drop bundle" }]
}
{% endschema %}
