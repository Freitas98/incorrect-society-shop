<!doctype html>
<html lang="{{ request.locale.iso_code }}">
  <head>
    {% render 'css-variables' %}
    {{ 'critical.css' | asset_url | stylesheet_tag: preload: true }}
  {% render 'meta-tags' %}
  {{ content_for_header }}
    <style>
      body {
        background: #000;
        color: #fff;
        font-weight: bold;
        font-family: var(--font-primary-family);
        min-height: 100vh;
        margin: 0;
        display: flex;
        align-items: center;
        justify-content: center;
      }
      .password-outer {
        width: 90%;
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        text-align: center;
      }
      .password-logo {
        margin-bottom: 2rem;
        max-width: 650px;
        width: 100%;
        filter: drop-shadow(0 0 8px #fff2);
      }
      .password-message {
        font-size: 3rem;
        margin: 2rem 0 1.5rem 0;
        letter-spacing: 0.5px;
        text-shadow: 0 1px 0 #222, 0 2px 8px #000a;
      }
      .social-icons {
        margin-top: 1.5rem;
        justify-content: center;
      }
      .password-bg-video {
        position: fixed;
        z-index: -2;
        right: 0;
        bottom: 0;
        min-width: 100%;
        min-height: 100%;
        object-fit: fill;
      }
      .password-overlay {
        position: fixed;
        z-index: -1;
        top: 0;
        left: 0;
        width: 100vw;
        height: 100vh;
        background: rgba(0, 0, 0, 0.55);
      }
    </style>
    <meta name="google-site-verification" content="cQT3m4WAClIG7LVSb9jwTrsBaW3XHArBN_kgB6ZfcUg" />
  </head>
  <body>
    {% comment %} <div style="display: none;">{{ content_for_layout }}</div> {% endcomment %}
    <video
      class="password-bg-video"
      autoplay
      loop
      muted
      playsinline
      defaultMuted
      oncontextmenu="return false;"
      preload="auto"
      id="myVideo"
    >
      <source src="https://cdn.shopify.com/videos/c/o/v/b79acf46bb6f498ca0023be1e5cb8408.mp4" type="video/mp4">
    </video>
    <script>
      document.addEventListener('DOMContentLoaded', function () {
        var video = document.getElementById('myVideo');
        if (!video) return;
        function isMobile() {
          return /Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent);
        }
        if (isMobile()) {
          video.muted = true;
          video.volume = 0;
          video.play();
        } else {
          video.muted = false;
          video.volume = 0.1;
          setTimeout(function () {
            video.play().catch(function (e) {
              video.muted = true;
              video.volume = 0;
              video.play();
            });
          }, 2500);
        }

        // On any user interaction, unmute and play video with sound if not already
        function globalTryPlayVideo() {
          if (video.muted || video.volume === 0) {
            video.muted = false;
            video.volume = 0.1;
            video.play().catch(function () {});
          }
        }
        document.addEventListener('click', globalTryPlayVideo, true);
        document.addEventListener('touchstart', globalTryPlayVideo, true);
        document.addEventListener('keydown', globalTryPlayVideo, true);
        // Black Friday countdown
        if (document.getElementById('countdown-timer')) {
          const targetDate = new Date('2025-11-28T00:00:00').getTime();
          function updateCountdown() {
            const now = new Date().getTime();
            const timeLeft = targetDate - now;
            if (timeLeft <= 0) {
              document.getElementById('bf-section').innerHTML = '<button id="bf-started-btn">Black Friday Started</button><p class="landing-subtext">Exclusive drops. Premium craft. Follow us and be first to enter the store when the collection opens.</p>';
              return;
            }
            const days = Math.floor(timeLeft / (1000 * 60 * 60 * 24));
            const hours = Math.floor((timeLeft % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
            const minutes = Math.floor((timeLeft % (1000 * 60 * 60)) / (1000 * 60));
            const seconds = Math.floor((timeLeft % (1000 * 60)) / 1000);
            document.getElementById('days').textContent = days.toString().padStart(2, '0');
            document.getElementById('hours').textContent = hours.toString().padStart(2, '0');
            document.getElementById('minutes').textContent = minutes.toString().padStart(2, '0');
            document.getElementById('seconds').textContent = seconds.toString().padStart(2, '0');
          }
          updateCountdown();
          setInterval(updateCountdown, 1000);
        }
      });
    </script>
    <div class="password-overlay"></div>
    <div class="password-outer">
      <img width="500" height="500" src="{{ 'logo.png' | asset_url }}" alt="Logo" class="password-logo" loading="lazy">
      {% if settings.black_friday_countdown %}
      <div id="bf-section">
        <div id="countdown-timer" class="countdown-timer">
          <div class="countdown-item">
            <span id="days">00</span>
            <span class="countdown-label">Days</span>
          </div>
          <div class="countdown-item">
            <span id="hours">00</span>
            <span class="countdown-label">Hours</span>
          </div>
          <div class="countdown-item">
            <span id="minutes">00</span>
            <span class="countdown-label">Minutes</span>
          </div>
          <div class="countdown-item">
            <span id="seconds">00</span>
            <span class="countdown-label">Seconds</span>
          </div>
        </div>
        <h1 class="landing-heading">{{ settings.black_friday_countdown_title }}</h1>
        <p class="landing-subtext">{{ settings.black_friday_countdown_subtitle }}</p>
      </div>
      {% else %}
      <h1 class="landing-heading">Preparing the next way fire drop</h1>
      <p class="landing-subtext">Exclusive drops. Premium craft. Follow us and be first to enter the store when the collection opens.</p>
      {% endif %}
      <div class="u-mt-40">
        {% form 'storefront_password' %}
          <input
            class="passwordTextBox"
            name="password"
            type="password"
            autocomplete="current-password"
            placeholder="Password"
            required
            id="passwordInput"
            value="IncorrectFriday.2025"
          >
          <button type="submit" class="btn btn--outline btn--lg password-btn-main" id="passwordEnterBtn">Enter</button>
        {% endform %}
      </div>
      <div class="landing-social">
        {% render 'social-icons', size: 48 %}
      </div>
    </div>
    <style>
      .landing-social {
        margin-top: 10px;
      }
      .landing-heading {
        font-size: 2.5rem;
        margin-bottom: 10px;
        letter-spacing: 1px;
        text-shadow: 0 1px 0 #222, 0 2px 8px #000a;
      }
      .password-btn-main {
        margin-top: 35px;
        --btn-padding-y: 0.85rem;
        --btn-padding-x: 2.4rem;
        --btn-border: color-mix(in oklab, var(--color-foreground, #fff), transparent 20%);
        --btn-color: var(--color-foreground, #fff);
        --btn-hover-bg: color-mix(in oklab, var(--color-foreground, #fff), transparent 88%);
        --btn-hover-border: var(--color-foreground, #fff);
        --btn-radius: 4px;
        letter-spacing: 0.12em;
      }
      .passwordTextBox {
        width: 100%;
        max-width: 340px;
        font-size: 1.2rem;
        padding: 16px 14px;
        border: 1px solid #fff;
        background: transparent;
        color: #fff;
        border-radius: 4px;
        font-family: var(--font-primary-family);
        margin-bottom: 0.5rem;
        box-sizing: border-box;
        display: none;
      }
      .passwordTextBox::placeholder {
        color: #fff;
      }
      .countdown-timer {
        display: flex;
        justify-content: center;
        gap: 1rem;
        margin: 2rem 0 3rem 0;
        font-size: 2rem;
        font-weight: bold;
        text-shadow: 0 1px 0 #222, 0 2px 8px #000a;
        font-family: 'Courier New', monospace;
      }
      .countdown-item {
        display: flex;
        flex-direction: column;
        align-items: center;
      }
      .countdown-label {
        font-size: 0.8rem;
        margin-top: 0.5rem;
      }
      #bf-started-btn {
        font-size: 2rem;
        margin: 2rem 0 1.5rem 0;
        padding: 1rem 2rem;
        background: transparent;
        border: 2px solid #fff;
        color: #fff;
        border-radius: 4px;
        cursor: pointer;
        text-shadow: 0 1px 0 #222, 0 2px 8px #000a;
        font-family: var(--font-primary-family);
      }
    </style>
  </body>
</html>
