{% comment %}
  Contact Us Page - Portuguese (pt-PT)
  This page provides contact information and a simple contact form for users to reach out to Incorrect Society.
{% endcomment %}

<section class="contact-page contact-card">
  <h1 class="contact-heading">Contacta-nos</h1>
  <p class="contact-intro">
    Se tiveres dúvidas, sugestões ou precisares de ajuda, preenche o formulário abaixo ou envia-nos um email para
    <a href="mailto:info@incorrectsociety.com" class="contact-highlight-link"
      >info@incorrectsociety.com</a
    >.<br>
    A nossa equipa responde normalmente em 1-2 dias úteis.
  </p>
  {% form 'contact', id: 'contact_form', class: 'contact-form', onsubmit: 'if(window.loadingScreen) window.loadingScreen.show()' %}
    {% if form.posted_successfully? %}
      <p class="form-success">
        Obrigado por nos contactares. Responderemos o mais breve possível.
      </p>
    {% endif %}
    
    {{ form.errors | default_errors }}

    <label for="ContactFormName">Nome</label>
    <input
      type="text"
      id="ContactFormName"
      name="contact[name]"
      required
      class="contact-field"
      placeholder="O teu nome"
      value="{% if form.name %}{{ form.name }}{% elsif customer %}{{ customer.name }}{% endif %}"
    >
    <label for="ContactFormEmail">Email</label>
    <input
      type="email"
      id="ContactFormEmail"
      name="contact[email]"
      required
      class="contact-field"
      placeholder="O teu email"
      value="{% if form.email %}{{ form.email }}{% elsif customer %}{{ customer.email }}{% endif %}"
    >
    <label for="ContactFormMessage">Mensagem</label>
    <textarea
      id="ContactFormMessage"
      name="contact[body]"
      rows="5"
      required
      class="contact-field"
      placeholder="Como podemos ajudar?"
    >{% if form.body %}{{ form.body }}{% endif %}</textarea>
    <button type="submit" class="btn btn--primary btn--block contact-submit">
      Enviar mensagem
    </button>
  {% endform %}
  <div class="contact-social-links">
    <strong>Redes sociais:</strong><br>
    <a href="https://www.instagram.com/incorrect.society/" target="_blank"
      >Instagram</a
    >
    <a href="https://www.tiktok.com/@.incorrectsociety" target="_blank"
      >TikTok</a
    >
    <a href="https://www.youtube.com/@IncorrectSociety" target="_blank"
      >YouTube</a
    >
  </div>
</section>
